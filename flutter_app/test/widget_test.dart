import 'package:flutter_test/flutter_test.dart';
import 'package:gamepass_uz/main.dart';

void main() {
  testWidgets('PlayPassApp smoke test', (WidgetTester tester) async {
    expect(PlayPassApp, isNotNull);
  });
}
